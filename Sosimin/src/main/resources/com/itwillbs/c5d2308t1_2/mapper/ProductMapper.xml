<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.c5d2308t1_2.mapper.ProductMapper">
	<!-- 상품 목록 갯수 -->
	<select id="productListCount" resultType="int">
		SELECT COUNT(*)
		FROM Product
		<if test="sId != null or !category.equals('') or !keyword.equals('') or !price.equals('') or !status.equals('')">
			WHERE 
		</if>
		<!-- 로그인시 회원 구별 조회 -->
		<if test="sId != null" >
			gu = (SELECT SUBSTRING_INDEX(SUBSTRING_INDEX(member_address, ' ', -2), ' ', 1) AS member_address
			FROM Member
			WHERE member_id = #{sId})		
		</if>
		<if test="sId != null and !category.equals('') or sId != null and !keyword.equals('')">
			AND
		</if>
		<!-- 카테고리 및 키워드 별 조회 -->
		<choose>
			<when test="!category.equals('')">
				product_category = #{category}	
			</when>
			<when test="!keyword.equals('')" >
				product_name LIKE CONCAT('%', #{keyword}, '%') 
			</when>
		</choose>
		<!-- 상품 가격별 조회 -->
		<if test="(sId != null or !category.equals('') or !keyword.equals('')) and !price.equals('')">
			AND
		</if>
		<if test="!price.equals('')">product_price between #{minPrice} AND #{maxPrice}</if>
		<!-- 상품 상태별 조회 -->
		<if test="(sId != null or !category.equals('') or !keyword.equals('')) and !status.equals('')">
			AND
		</if>
		<if test="!status.equals('')">
			<choose>
				<when test="status3 != null">
					product_status IN (#{status1}, #{status2}, #{status3})
				</when>
				<when test="status2 != null">
					product_status IN (#{status1}, #{status2})
				</when>
				<when test="status1 != null">
					product_status = #{status1}
				</when>
				<otherwise>
					1 + 1
				</otherwise>
			</choose>
		</if>
		
	</select>
	
	<!-- 상품 목록 조회 -->
	<select id="selectProductList" resultType="map"> 
		SELECT p.product_id, product_category, p.member_id, product_board_id, product_name, product_price, gu, p.dong  
		, product_datetime, product_image1
		FROM Product p JOIN Product_board pb
		ON p.product_id = pb.product_id
		JOIN Member m
		ON p.member_id = m.member_id
		<if test="sId != null or category != '' or keyword != '' or price != '' or status != ''">
			WHERE 
		</if>
		<if test="sId != null" >
			gu = (SELECT SUBSTRING_INDEX(SUBSTRING_INDEX(member_address, ' ', -2), ' ', 1) AS member_address
			FROM Member
			WHERE member_id = #{sId})		
		</if>
		<!-- 카테고리 및 키워드 별 조회 -->
		<if test="sId != null and category != '' or sId != null and keyword != ''">
			AND
		</if>
		<choose>
			<when test="category != ''">
				product_category = #{category}	
			</when>
			<when test="keyword != ''" >
				product_name LIKE CONCAT('%', #{keyword}, '%') 
			</when>
		</choose>
		<!-- 상품 가격별 조회 -->
		<if test="(sId != null or !category.equals('') or !keyword.equals('')) and !price.equals('')">
			AND
		</if>
		<if test="!price.equals('')">product_price between #{minPrice} AND #{maxPrice}</if>
		<!-- 상품 상태별 조회 -->
		<if test="(sId != null or !category.equals('') or !keyword.equals('')) and !status.equals('')">
			AND
		</if>
		<if test="!status.equals('')">
			<choose>
				<when test="status3 != null">
					product_status IN (#{status1}, #{status2}, #{status3})
				</when>
				<when test="status2 != null">
					product_status IN (#{status1}, #{status2})
				</when>
				<when test="status1 != null">
					product_status = #{status1}
				</when>
				<otherwise>
					1 + 1
				</otherwise>
			</choose>
		</if>
		ORDER BY 
		<choose>
			<when test="sort == 1">product_datetime DESC,</when>
			<when test="sort == 2">product_wishcount DESC ,</when>
			<when test="sort == 3">product_price ASC,</when>
			<when test="sort == 4">product_price DESC,</when>
		</choose>
		product_datetime DESC
		LIMIT #{page.startRow}, #{page.listLimit}
	</select>
	
	
	
	<!-- ================================================================================= -->
	<!-- 상품 등록 - INSERT -->
	<insert id="productInsert">
		INSERT INTO Product
		VALUES (
			null
			, 'admin'
			, #{product_name}
			, #{product_price}
			, #{product_txt}
			, #{product_category}
			, #{product_image1} 
	        , <if test="product_image2 != null">#{product_image2}</if><if test="product_image2 == null">NULL</if> 
	        , <if test="product_image3 != null">#{product_image3}</if><if test="product_image3 == null">NULL</if>
	        , <if test="product_image4 != null">#{product_image4}</if><if test="product_image4 == null">NULL</if>
	        , <if test="product_image5 != null">#{product_image5}</if><if test="product_image5 == null">NULL</if>
			, now()
			, #{trade_method}
			, #{product_status}
			, #{gu}
			, #{dong}
			, 0
			, #{tag_name1}
			, #{tag_name2}
			, #{tag_name3}
			, #{tag_name4}
		)
	</insert>	
	<insert id="productBoardInsert">
		INSERT INTO Product_board
		values (
			null
			,(SELECT max(product_id)
			 FROM Product)
			,0
			,0
			,0
		);
	</insert>
	<select id="selectProductId" resultType="int">
		SELECT max(product_id)
		 FROM Product
	</select>
		
	<!-- Map으로 받은 상품 -->
	<select id="selectProduct" resultType="hashMap">
		SELECT *
		FROM Product
		WHERE product_id = #{product_id};
	</select>

	<!-- 날짜 데이터 가공을 위해 LIST로 받은 상품목록 -->	
	<select id="selectProduct2" resultType="map">
		SELECT *
		FROM Product
		WHERE product_id = #{product_id};
	</select>

	<!-- 전체상품 조회 -->
<!-- 		<select id="selectAllProduct" resultType="map"> -->
<!-- 		SELECT * -->
<!-- 		FROM Product -->
<!-- 		</select> -->
	<!-- 연관상품 조회 -->
	<select id="selectRelatedProducts" resultType="map">
    SELECT *
    FROM Product
    WHERE (tag_name1 LIKE CONCAT('%', #{tag_name1}, '%') OR product_name LIKE CONCAT('%', #{product_name}, '%')) AND product_id != #{product_id}
	</select>
	
	
<!-- 	<select id="selectRelatedProducts" resultType="hashMap"> -->
<!-- 		SELECT * -->
<!-- 		FROM Product -->
<!-- 		WHERE tag_name LIKE '%아이유%' OR product_name LIKE '%#{product_name}%'; -->
<!-- 	</select> -->
</mapper>